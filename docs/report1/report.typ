#set text(font: "Noto Sans CJK SC")
#set heading(numbering: "1.1")

#set document(
  author: "Bangyan Gong <crvenabombarder@sjtu.edu.cn>",
  title: "作业1：基于OpenGL的室内场景实现",
)

#align(center)[
#text(size: 16pt)[
*基于OpenGL的室内场景实现*
]

龚邦彦 \ 523021910130
]

#align(center)[
#set par(justify: false)
*摘要* \
本项目基于 SimpleScene 程序框架，使用 OpenGL 技术搭建了一个包含中式元素的室内场景。报告将阐述项目的实现过程，其中涵盖的关键技术点包括：利用几何变换调整场景尺寸、应用 Blinn-Phong 光照模型、通过纹理映射与 Alpha 测试实现中式镂空窗效果，以及使用 Assimp 库加载并渲染外部 3D 模型。此外，在开发流程上，项目采用了 Meson 作为构建系统，并利用 Nix 包管理器来确保开发环境的一致性与可复现性。
]

= 引言

== 作业背景

本次作业要求有：

- 基于 SimpleScene 程序框架和初始场景，搭建OpenGL编程环境和绘图窗口。
- 在场景中相机正面的墙面上绘制一个中式木质镂空窗，并改变场景比例或色彩。
- 加载中式书桌模型，并在场景中合理摆放。

== 实现概述

为完成作业要求，本项目进行了以下主要工作：

1. *环境配置与构建*：配置了基于 GLAD、GLFW 和 GLM 的 OpenGL 开发环境。项目构建采用 Meson 构建系统，开发环境则通过 Nix 进行管理，以保证依赖的统一和稳定。
2. *场景布局调整*：通过平移和缩放变换，调整了墙面、地板等对象的位置和尺寸，使场景空间更显协调。
3. *光照模型应用*：为场景中的墙体等对象应用了 Blinn-Phong 光照模型，以计算更真实的镜面高光。
4. *中式镂空窗实现*：利用带 Alpha 通道的纹理，结合着色器中的 Alpha 测试（`discard` 操作），在墙面上实现了镂空效果。
5. *外部模型加载*：集成了 Assimp 库，并将其封装为 `Model` 和 `Mesh` 类，用于加载和渲染 Wavefront `.obj` 格式的中式书桌模型。

= 运行环境与开发工具

== 依赖库

本项目使用了以下依赖j：

- *OpenGL/GLAD*: 用于访问 OpenGL API。
- *GLFW*: 用于创建窗口、上下文及处理用户输入。
- *GLM*: 提供向量和矩阵运算功能。
- *Assimp*: 用于导入 3D 模型文件。
- *stb_image.h*: 用于加载纹理图像。

== 构建系统：Meson

项目采用 Meson 作为构建系统。通过编写 `meson.build` 文件定义项目结构、源文件和依赖项，实现了跨平台的快速构建。

== 开发及构建环境：Nix

为确保环境的一致性，本项目利用 Nix 包管理器创建了一个声明式的开发环境。 Nix 通过隔离的、可复现的构建过程，确保了在不同机器上环境的统一性，避免了“在我机器上能跑”的问题。

== Nix 环境定义与构建

通过 Nix 实现项目的可复现构建主要分为两步：定义构建过程和打包成容器镜像。

1. *应用程序构建*
   `default.nix` 文件定义了如何编译 OpenGL 应用程序。它声明了所有必要的依赖项，如 `glfw`、`glm` 等图形库，以及 `cmake`、`meson` 等编译工具。文件详细描述了配置、编译和安装的各个阶段，最终生成一个包含可执行文件的 Nix 构建产物（derivation）。

2. *容器镜像打包*
   `image.nix` 文件则负责将前一步构建好的应用程序打包成一个容器镜像。其工作流程如下：
   - 首先，导入 `default.nix` 以获取应用程序的构建结果。
   - 接着，引入 `nixGL`，这是一个用于在容器内桥接主机 OpenGL 驱动的工具，解决了在容器中运行图形应用时常见的驱动问题。
   - 然后，通过 `pkgs.buildEnv` 创建一个包含应用程序和 `nixGL` 的统一环境。
   - 最后，利用 Nixpkgs 提供的 `dockerTools.buildImage` 函数，将这个统一环境作为内容层（contents）打包成一个镜像。镜像的默认启动命令被设置为运行我们的 OpenGL 程序。

通过这种方式，整个编译和打包过程都是声明式和自动化的，确保了从源码到最终容器镜像的每一步都清晰可控且高度可复现。

= 场景搭建与变换

== 基础场景框架

项目基于 `simple_scene` 框架，该框架提供了一个基本的窗口和渲染循环。

== 场景尺寸变换

为使场景比例协调，我们对构成房间的六个面（墙体、天花板、地板）进行了几何变换。这主要通过 GLM 库提供的变换矩阵来实现。例如，为加宽房间，将左右墙壁的 `model` 矩阵在 X 轴方向上进行了平移，并将前后墙壁及天花板、地板的 `model` 矩阵在 X 轴方向上进行了缩放。
变换过程可由以下矩阵乘法表示：

$
M_"final" = M_"translate" dot.c M_"rotate" dot.c M_"scale"
$

通过以上变换，调整了房间的整体布局。

= 核心技术方案

== Blinn-Phong 光照模型

为提升场景的真实感，墙体、地板等物体的光照计算采用了 Blinn-Phong 模型。该模型主要在镜面反射光的计算上进行了改进。

1. *环境光 (Ambient)*: 模拟场景中的间接光照。

   $
   C_"ambient" = M_"ambient" dot.c L_"ambient"
   $

   其中，$M_"ambient"$是材质的环境光颜色，$L_"ambient"$是光源的环境光强度。

2. *漫反射 (Diffuse)*: 模拟光线在粗糙物体表面的散射。

   $
   C_"diffuse" = M_"diffuse" dot.c L_"diffuse" dot.c max(bold(N dot.c bold(L)), 0)
   $

   其中，$bold(N)$是表面法线向量，$bold(L)$是指向光源的向量。

3. *镜面反射 (Specular)*: Blinn-Phong 模型引入“半程向量” ($bold(H)$) 来计算镜面高光。
   $
   bold(H) = "normalize"(bold(L) + bold(V))
   $
   其中，$bold(V)$是指向相机的视线向量。镜面反射强度取决于法线 $bold(N)$ 和半程向量 $bold(H)$ 之间的夹角。
   $
   C_"specular" = M_"specular" dot.c L_"specular" dot.c (max(bold(N) dot.c bold(H), 0))^"shininess"
   $
   与 Phong 模型相比，半程向量的计算通常更快，且能产生更柔和的高光。

最终的颜色是以上三个分量的总和：

$
C_"final" = C_"ambient" + C_"diffuse" + C_"specular"
$

== 中式镂空窗的实现

中式镂空窗的效果通过纹理映射和 Alpha 测试技术实现。

1. *图形表示与纹理映射*:
   使用了一张带透明通道（Alpha Channel）的 PNG 图片作为窗户的纹理。图片中窗框部分不透明，镂空部分完全透明。通过 UV 映射技术将该 2D 图片贴在 3D 墙面上。

   为控制窗户在墙面上的大小和位置，在顶点着色器中对纹理坐标进行了线性变换：

   $
   "uv"_"final" = "uv"_"original" dot.c bold(S)_"tex" + bold(O)_"tex"
   $

   其中 $bold(S)_"tex"$ 是缩放因子，$bold(O)_"tex"$ 是偏移量。

2. *透明效果与Alpha测试*:
   在片段着色器中，根据从纹理中采样得到的 Alpha 值来决定片段的输出。
   - 对于 Alpha 值较高的区域（窗框），其颜色由 Blinn-Phong 光照计算结果与纹理颜色混合而成。
   - 对于 Alpha 值趋近于 0 的区域（镂空部分），使用 `discard` 关键字。该指令会直接丢弃当前片段，不将其写入帧缓冲，从而显示出墙体后面的内容，实现“镂空”效果。

== 模型的加载与渲染

为了在场景中加入中式书桌，使用了 Assimp 库来加载外部 3D 模型文件。

1. *Assimp模型加载*: Assimp 库能够解析多种 3D 模型文件格式，并将其数据转换为统一的结构（`aiScene`）。程序通过 Assimp 接口将书桌模型文件读入内存。

2. *Model与Mesh类封装*:
   为了管理模型数据，将模型结构封装在 `Model` 和 `Mesh` 两个类中。
   - *`Mesh` 类*: 代表模型的一个可绘制部分，存储了顶点属性、索引和材质纹理，并负责管理自身的 VAO、VBO 等 OpenGL 对象。
   - *`Model` 类*: 代表一个完整的模型，可能由一个或多个 `Mesh` 组成。`Model` 类负责调用 Assimp 加载模型，并将 Assimp 的数据结构转换为程序内部的 `Mesh` 对象。

3. *模型渲染*: 在渲染循环中，调用书桌 `Model` 对象的 `Draw()` 方法，该方法会遍历其包含的所有 `Mesh` 对象，并依次完成绘制。

= 实验结果与分析

#figure(caption: [最终结果])[
#image("resources/scene.png")
]

#figure(caption: [镂空窗效果展示])[
#image("resources/window.png")
]

从最终渲染结果来看：

- *场景布局*: 房间空间感适中，墙面颜色、地板材质与光照共同营造了和谐的室内氛围。
- *光照效果*: Blinn-Phong 模型在墙面和书桌上产生了较为柔和自然的高光，增强了场景的立体感。
- *镂空窗*: 窗户的镂空效果符合预期，通过透明区域可以看到墙体后方，丰富了视觉层次。
- *书桌模型*: 中式书桌模型被正确加载并渲染，其纹理和材质在光照下表现正常，丰富了场景内容。

= 总结

本次作业综合运用了 OpenGL 的多项技术，完成了一个具有特定风格的室内场景。在实现过程中，巩固了对三维变换、光照模型、纹理映射等知识的理解，并实践了使用 Assimp 库加载模型的方法。

在工程实践方面，采用 Meson 和 Nix 管理项目构建与开发环境，为保证项目的一致性和可复现性提供了支持。总体而言，本次作业覆盖了从场景设计、技术实现到环境部署的完整流程，为后续更复杂的图形学项目打下了基础。
